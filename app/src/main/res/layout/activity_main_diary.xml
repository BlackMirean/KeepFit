<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/back_button_maindiary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:title="训练日记"
        app:titleTextColor="@color/white"
        android:background="@color/bgcolor"
        app:navigationIcon="@drawable/arrow_back"
        />

    <LinearLayout
        android:id="@+id/linearlayouy_shop_recy"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/back_button_maindiary"
        android:orientation="vertical">

        <!-- 这里可以添加其他的子视图 -->

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recy_diary"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/white" />


        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:gravity="center"
            android:orientation="horizontal">


            <Button
                android:layout_margin="10dp"
                android:id="@+id/btn_adddiary"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/btn_diary"
                android:textColor="@color/white"
                android:textSize="20sp"
                android:text="添加日记"
                />

        </LinearLayout>

    </LinearLayout>

</androidx.appcompat.widget.LinearLayoutCompat>
